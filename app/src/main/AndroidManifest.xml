<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imsihyun.android_server_hapdong">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ApplicationController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name_3" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name_1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignUpActivity"
            android:label="@string/app_name_2" />
        <activity android:name=".MainMenuHansikFragment" />
        <activity android:name=".MainMenuChickenFragment" />
        <activity android:name=".MainMenuPizzaFragment" />
        <activity android:name=".MainMenuYasikFragment"></activity>
    </application>

</manifest>